'use babel';

import Laravel5SnippetsAtomView from '../lib/laravel5-snippets-atom-view';

describe('Laravel5SnippetsAtomView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
